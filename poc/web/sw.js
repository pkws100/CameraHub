const CACHE='pkws-camera-hub-v1.5.0-dev';
const STATIC=new Set(['/','/index.html','/display.html','/styles.css','/discovery.css','/media-modes.css','/users.css','/camera-connections.css','/audit-fixes.css','/wall-mode.css','/display-profiles.css','/display-device.css','/auth-badges.css','/operations.css','/detection.css','/app.js','/display-device.js','/reader.js','/manifest.webmanifest','/icons/icon-192.png','/icons/icon-512.png']);
self.addEventListener('install',event=>{event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll([...STATIC])));self.skipWaiting();});
self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key)))));self.clients.claim();});
self.addEventListener('fetch',event=>{const url=new URL(event.request.url);if(event.request.method!=='GET'||url.origin!==self.location.origin)return;if(url.pathname.startsWith('/api/')||url.pathname.startsWith('/internal/')||url.pathname.startsWith('/whep/')||url.pathname.startsWith('/hls/')||event.request.destination==='video'||event.request.headers.has('range'))return;if(event.request.mode==='navigate'){event.respondWith(fetch(event.request).catch(()=>caches.match('/index.html')));return;}if(!STATIC.has(url.pathname))return;event.respondWith(fetch(event.request).then(response=>{if(response.ok)caches.open(CACHE).then(cache=>cache.put(event.request,response.clone()));return response;}).catch(()=>caches.match(event.request)));});
